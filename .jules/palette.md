## 2024-07-03 - Accessible buttons loading states
**Learning:** Using `Loader2` inside an icon button to replace the main icon during loading states is a very effective pattern for conveying background operations like following/unfollowing a playlist to screen readers and visual users without layout shifts.
**Action:** Use this pattern instead of placing a loader outside or changing the button text.
